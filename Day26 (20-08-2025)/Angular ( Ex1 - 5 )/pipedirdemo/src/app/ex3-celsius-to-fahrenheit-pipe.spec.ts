import { Ex3CelsiusToFahrenheitPipe } from './ex3-celsius-to-fahrenheit-pipe';

describe('Ex3celsiusToFahrenheitPipe', () => {
  it('create an instance', () => {
    const pipe = new Ex3CelsiusToFahrenheitPipe();
    expect(pipe).toBeTruthy();
  });
});
